---
interface Props {
  eyebrow: string;
  title: string;
  summary?: string;
}

const { eyebrow, title, summary } = Astro.props;
---

<div class="section-heading">
  <span>{eyebrow}</span>
  <h2>{title}</h2>
  {summary && <p>{summary}</p>}
</div>
